// src/components/Section/ApostlesCreed/apostlesCreedHelpers.js

// 예시: 선택된 버전이 유효한지 확인하는 함수 (필요 시 사용)
export const validateVersion = (version) => {
    return version === 'Old' || version === 'New';
};